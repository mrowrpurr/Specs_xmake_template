#pragma once

// This includes the basic Specs DSL, things like 'Test' and 'Setup' and 'Teardown'
#include <Specs.h>

// (Optional) This includes the Snowhouse library and the Specs adapter for it
#include <Specs/Snowhouse.h>

// (Optional) This includes the libassert library and the Specs adapter for it
#include <Specs/LibAssert.h>
